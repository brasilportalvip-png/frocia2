# Baseline reproduzível — 26/08/2026

## Escopo

- Repositório: `brasilportalvip-png/frocia2`
- Commit de baseline: `d1f0c66`
- Commit da primeira fundação do Prompt Mestre: `c97865e`
- Branch de trabalho: `feat/prompt-master-foundation`
- Plataforma de hospedagem observada no PR: Vercel

Este relatório substitui declarações antigas que não apontavam para o commit e o ambiente realmente executados.

## Inventário do baseline

- 185 arquivos rastreados pelo Git.
- 78 arquivos no backend `server/`.
- 46 arquivos no frontend `src/`.
- 21 arquivos de teste antes desta fase.
- API Express, frontend React/Vite, Firebase/Firestore, Gemini e Mercado Pago presentes no código.
- CI configurado em `.github/workflows/ci.yml`.
- Deploy configurado em `vercel.json` e `api/`.

## Reprodução do commit `d1f0c66`

Ambiente local de auditoria:

- Node.js `v24.19.0`.
- npm `11.9.0`.
- O projeto exige Node.js `>=22.12.0`.

Resultados:

| Gate | Comando | Resultado |
|---|---|---|
| Instalação limpa | `npm --cache /tmp/frocia-ci-cache ci --ignore-scripts` | 470 pacotes instalados; sucesso após isolar o cache do executor |
| Tipagem | `tsc --noEmit` | Exit 0 |
| Testes do baseline | `vitest run` | 21 arquivos e 148 testes aprovados |
| Build do baseline | `vite build` + bundle do servidor com esbuild | Exit 0 |
| Auditoria de produção | `npm audit --omit=dev --audit-level=high` | Exit 0; 6 vulnerabilidades moderadas transitivas; nenhuma alta ou crítica |
| Tracker | `node --import tsx scripts/validate-requirement-tracker.ts` | 563 requisitos e 563 IDs únicos |

A primeira tentativa de instalação falhou porque o executor tentou gravar em `/root/.npm`. A repetição com cache isolado funcionou. Essa falha pertence ao ambiente de auditoria, não ao repositório.

## Evidência externa disponível

- O PR nº 1 mostrou 3/3 checks aprovados para o commit `450fb98`.
- O preview Vercel abriu a interface.
- O preview respondeu `status: live` em `/api/live`.
- O preview respondeu `status: ready` em `/api/ready`, com autenticação, Firestore, Gemini e Mercado Pago ativos.
- O proprietário realizou o merge do PR nº 1.

As respostas do preview foram observadas e fornecidas pelo proprietário. Elas ainda não constituem revisão independente do conjunto completo de requisitos.

## Estado honesto do Prompt Mestre no baseline

| Estado | Quantidade |
|---|---:|
| `OPEN` | 514 |
| `IN_PROGRESS` | 16 |
| `FIXED_NOT_VERIFIED` | 33 |
| `VERIFIED` | 0 |
| Total | 563 |

## Primeiro bloco implementado em `c97865e`

- Classificador de domínio, complexidade, sensibilidade e necessidade de pesquisa.
- Orquestrador integrado às execuções síncrona e streaming.
- Tratamento reforçado para saúde, jurídico e financeiro.
- Contratos operacionais completos para as quatro ferramentas registradas.
- Rejeição fail-closed de ferramentas desconhecidas.
- 9 testes novos do bloco; 157/157 testes totais aprovados.
- Typecheck, build e `git diff --check` aprovados.

Estado do tracker após registrar esta entrega:

| Estado | Quantidade |
|---|---:|
| `OPEN` | 473 |
| `IN_PROGRESS` | 42 |
| `FIXED_NOT_VERIFIED` | 48 |
| `VERIFIED` | 0 |
| Total | 563 |

`FIXED_NOT_VERIFIED` significa que há implementação e teste local, mas ainda falta revisão independente e/ou evidência no ambiente aplicável. Não significa produção validada.

## Segundo bloco implementado em `5de3d06`

- Runtime de ferramentas com autorização por escopo e confirmação humana.
- Validação de entrada e saída com falha fechada.
- Timeout, cancelamento e retry somente para falhas explicitamente transitórias.
- Limite de custo e rate limit por ferramenta, usuário e projeto.
- Redaction recursiva de recibos e logs estruturados.
- Ledger de idempotência com adaptadores local e Firestore.
- Owner token e lease impedem worker antigo de concluir.
- Resultado mutável incerto vira `external_blocker` e não é repetido automaticamente.
- 16 testes específicos do runtime; 173/173 testes totais aprovados.

Estado do tracker após o segundo bloco:

| Estado | Quantidade |
|---|---:|
| `OPEN` | 465 |
| `IN_PROGRESS` | 40 |
| `FIXED_NOT_VERIFIED` | 58 |
| `VERIFIED` | 0 |
| Total | 563 |

Os handlers reais de publicação, pagamento e outros efeitos externos ainda precisam ser migrados para o runtime. O adaptador Firestore está implementado e compila, mas ainda não foi exercitado no Emulator ou em preview.

## Bloqueadores e riscos residuais

- Nenhum requisito possui ainda revisor independente e cadeia completa para receber `VERIFIED`.
- O smoke test da URL de produção após o merge ainda não foi registrado neste repositório.
- Permanecem 6 vulnerabilidades moderadas transitivas em dependências de produção.
- Testes E2E completos, acessibilidade, regressão visual, carga, backups, alertas e recuperação ainda não foram comprovados.
- Credenciais e integrações externas não foram auditadas diretamente por um verificador independente.

Conclusão: o baseline é executável e passou nos gates locais existentes, mas a plataforma completa do Prompt Mestre permanece em andamento.
